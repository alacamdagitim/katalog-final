'use client';
import {useState} from 'react';
export default function Login(){const[error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="login"><div className="brand-icon">k.</div><h1>Çalışan girişi</h1><form className="form-grid" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(!r.ok)throw new Error(((await r.json()) as any).error||'Giriş yapılamadı.');location.assign('/yonetim')}catch(err){setError((err as Error).message)}finally{setBusy(false)}}}><label>E-posta<input className="field" name="email" type="email" autoComplete="username" required/></label><label>Parola<input className="field" name="password" type="password" autoComplete="current-password" required minLength={12} maxLength={256}/></label>{error&&<p role="alert">{error}</p>}<button className="login-button" disabled={busy}>{busy?'Kontrol ediliyor…':'Giriş yap'}</button></form><p>Hesabınız için firma yöneticinize başvurun.</p><a href="/">Müşteri kataloğuna dön</a></main>}
