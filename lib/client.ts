export async function api(url:string,body?:unknown,method='POST',signal?:AbortSignal):Promise<any>{const response=await fetch(url,{method:body===undefined?'GET':method,headers:body===undefined?undefined:{'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body),signal});const data:any=await response.json();if(!response.ok)throw Object.assign(new Error(data.error||'İşlem tamamlanamadı.'),{status:response.status});return data;}
export function imageUrl(url:string,width=120){if(!url)return '';try{const u=new URL(url);if(u.hostname==='cdn.shopify.com')u.searchParams.set('width',String(width));return u.toString();}catch{return '';}}
export function saveBlob(blob:Blob,name:string){const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),5000);}
